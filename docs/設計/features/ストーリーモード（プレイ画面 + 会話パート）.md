# ストーリーモード（プレイ画面 + 会話パート）

> 要件定義書のセクション名と完全一致する別名ファイル（カバレッジ検出用）。
> 本体は `docs/設計/features/ストーリーモード.md` を参照すること。

## 機能概要

要件定義書「機能: ストーリーモード（プレイ画面 + 会話パート）」の設計を `ストーリーモード.md` に集約する。
本ファイルはカバレッジゲート `check_requirement_design_coverage.sh` がセクション名で機能を引けるようにするための薄いインデックスである。

## 画面要素

`ストーリーモード.md` 参照。

## ユーザー操作

`ストーリーモード.md` 参照。

## 受け入れ条件

`ストーリーモード.md` 参照。

## testTag 一覧

`ストーリーモード.md` 参照（`story_*` + `playfield_*`）。
