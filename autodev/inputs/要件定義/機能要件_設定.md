# 機能要件: 設定

## 機能概要

Settings 画面（DataStore 全 10 キー UI）。設計詳細は `docs/設計/features/設定.md` 参照。

## 画面要素

設計書参照（testTag 一覧）。

## ユーザー操作

設計書参照。

## エラーケース

設計書参照。ハンドリングは clazz 単位で例外を catch し AppLogger に warning を残す。

## データモデル

呼び出し側 ViewModel + Repository 経由（共通基盤_AppDatabase / 共通基盤_SettingsRepository）。

## 受け入れ条件

- Given: アプリ起動 + 当該画面（または統合機能トリガ）
- When: 設計書 §受け入れ条件 のシナリオを実機（emulator-5554）で実行
- Then: 期待動作が観察できる（Maestro smoke_title.yaml で title→settings/mode の navigate PASS、その他は consumer 動線 + AppDatabase 統合 smoke で検証）
